# Storybook 8 peer-requires vite ^4||^5||^6 but this app uses vite 8;
# the Vite build itself is unaffected. Skip npm strict peer enforcement
# until Storybook ships Vite 8 support.
legacy-peer-deps=true
