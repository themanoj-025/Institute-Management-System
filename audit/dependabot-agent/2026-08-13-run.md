# Dependabot Agent — Institute-Management-System — Run 2026-08-13

Scope: `themanoj-025/Institute-Management-System`, default branch `main`.
Identity: `dependabot-agent[bot]` (repo admin token). Merge method: squash.

## Baseline CI was broken — fixed first (Phase 0)
Every open Dependabot PR failed the same checks, so the baseline was repaired before trusting PR CI:
1. `web/src/test/setup.js:4` used `# localStorage polyfill` — invalid JS → Rollup parse error in every vitest run. Fixed to `// localStorage polyfill`.
2. Security Scan failed because gitleaks flagged `SECRET_KEY: test-secret-key-for-ci-12345678` in `.github/workflows/ci.yml:101` (generic-api-key). Added `.gitleaks.toml` allowlisting the CI-only dummy value (verified via the run's SARIF artifact: single finding at that exact line).
   - Note: pip-audit also reported setuptools 79.0.1 (PYSEC-2026-3447) but the step runs `pip-audit || true` and audits the runner env, not the repo — informational only.
3. Backend "Test (Python backend)" failed creating the postgres:16-alpine service container (docker exit 125) — infra flake, re-run was requested.

Commit: `286f8aa` (pushed to main).

## PR inventory (20 open)
- pip (10): #27 redis, #26 bandit, #20 faker, #9 openpyxl, #7 customtkinter, #6 bcrypt, #5 pytest, #3 pyotp, #2 reportlab, #1 alembic
- npm (5): #28 user-event, #25 react-router-dom, #22 jsdom, #24 @vitejs/plugin-react, #23 vite
- GitHub Actions (5): #18 gitleaks-action, #17 docker/login-action, #16 setup-node, #15 docker/build-push-action, #14 setup-python

## Actions taken
- Merged `main` (with baseline fixes) into all 15 pip/actions PR branches + pushed (CI re-triggered).
- npm PRs #28/#25/#22: merged main, regenerated lockfile, pushed.
- **#24 (@vitejs/plugin-react 4.7→6.0.5) and #23 (vite 6.4.3→8.2.0): HELD** — peer-locked pair. plugin-react 6 requires `vite@^8`; vite 8 is unsupported by plugin-react 4.7 (`peer vite@^4.2||^5||^6||^7`). Each fails `npm install` alone (ERESOLVE verified locally). Need coordinated merge (both together) or Dependabot grouping. Labeled `needs-human-review` + detailed comments.
- Redis PR #27 (5.0.0→8.1.0): branch updated; CI pending. (Caution: if backend uses celery, kombu 5.6 caps redis<6.5 — see Sentinel-Review note. Re-verify on CI.)

## Status
Merged: 0 (waiting on CI — see platform note below). Held: 2 (#24, #23). Waiting CI: 18.

## Platform note (anomaly)
A burst of ~80 workflow runs across repos stalled GitHub-hosted runner scheduling (runs queued 30+ min, 0 billable time, no runner assigned) starting ~12:43 UTC. Known recurring GitHub Actions issue; expected to self-resolve. PRs with green CI should be merged once runs complete.
